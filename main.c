#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
#include <windows.h>
#include <string.h>
#include "diccionario.h"
#include "motor.h"

int main()
{
    /*char* dict;
    int validos = 0;
    generarDiccionario(&dict, &validos, "try.txt");
    char frase[100];
    gets(frase);
    int validosFrase = strlen(frase);
    parola* arrFrase = separarFraseEnTerminos(frase, &validosFrase);
    for(int i=0; i<validos; i++)
    {
        printf("%s\n", arrFrase[i].palabra);
    }*/

    return 0;
}
